# Example .env.sample — DO NOT store real secrets here.
# Copy to .env and set real values locally or use a secrets manager.

# SQLALCHEMY_DATABASE_URL="postgresql://avnadmin:CHANGEME@pg-host:28191/defaultdb?sslmode=require"
